import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')


let arr = 'hello world'

console.log(arr.slice(6, 11));

let peoples = ['daler', 'ravhskan', 'amir', 'murod']

console.log(peoples.slice(0, 2));